# Створення прикладних програм на мові Python
# Лабораторна робота №4
# Marusiak Vasya
print ('[1] Створення прикладних програм на мові Python3, Лабораторна #4.1')
print ('[2] Marusiak Vasya, ')
epsilon = 10E-4
n = 1
a = 1 / 2
suma = a
while a > epsilon:
	n = n + 1 
	ch = 1
	znam = 1
	for x in range(1, n+1):
		ch = ch * x
	znam = 2 ** (n ** 2)
	a = ch**2 / znam
	suma += a
print('[3] При n = {},  a[n] = {} < {}, і сума всіх елементів буде рівна - {}'.format(n,a,epsilon,suma))
